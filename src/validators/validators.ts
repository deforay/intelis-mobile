import {
    trimmedCharsValidator
  } from '../validators/minLength.validator';
  import {
    PasswordValidator
  } from '../validators/password.validator';
  import {
    NoWhiteSpaceValidator
  } from '../validators/nowhitespace.validator';  
  import {
    EmailIdValidator
  } from '../validators/emailId.validator';
  import {
    MobileNoValidator
  } from '../validators/mobileno.validator';
export {
    trimmedCharsValidator,
    PasswordValidator,
    NoWhiteSpaceValidator,
    EmailIdValidator,
    MobileNoValidator,
}